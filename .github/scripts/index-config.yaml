# in the repos, the key specifies the name of the temp directory name
repos:
  rw-cli-codecollection: https://github.com/runwhen-contrib/rw-cli-codecollection.git
robot_file_pattern:
  codebundles: .robot