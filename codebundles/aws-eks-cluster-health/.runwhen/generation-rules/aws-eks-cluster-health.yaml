apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  platform: aws
  generationRules:
    - resourceTypes:
        - aws_eks_clusters
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: aws-eks-cluster-health
          qualifiers: ["resource"]
          baseTemplateName: aws-eks-cluster-health
          levelOfDetail: basic
          outputItems:
            - type: slx
            - type: runbook
              templateName: aws-eks-cluster-health-taskset.yaml
