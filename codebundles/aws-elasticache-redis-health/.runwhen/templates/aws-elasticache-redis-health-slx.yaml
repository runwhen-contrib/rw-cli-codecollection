apiVersion: runwhen.com/v1
kind: ServiceLevelX
metadata:
  name: {{slx_name}}
  labels:
    {% include "common-labels.yaml" %}
  annotations:
    {% include "common-annotations.yaml" %}
spec:
  imageURL: https://storage.googleapis.com/runwhen-nonprod-shared-images/icons/aws/elasticache.png
  alias: "{{match_resource.account_name}} AWS Elasticache Caches in {{match_resource.resource.region}}"
  asMeasuredBy: "The number of unavailable Elasticache serverless caches in account {{match_resource.account_name}} region {{match_resource.resource.region}}"
  configProvided:
  - name: SLX_PLACEHOLDER
    value: SLX_PLACEHOLDER
  owners:
  - {{workspace.owner_email}}
  statement: The number of unavailable Elasticache serverless caches should be 0. 
  additionalContext:
    {% include "aws-hierarchy.yaml" ignore missing %}
    qualified_name: "{{ match_resource.qualified_name | replace(":", "_") }}"
  tags: 
    {% include "aws-tags.yaml" ignore missing %}
    - name: access
      value: read-only