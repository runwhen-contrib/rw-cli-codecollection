apiVersion: runwhen.com/v1
kind: ServiceLevelIndicator
metadata:
  name: {{slx_name}}
  labels:
    {% include "common-labels.yaml" %}
  annotations:
    {% include "common-annotations.yaml" %}
spec:
  displayUnitsLong: OK
  displayUnitsShort: ok
  location: {{default_location}}
  description: Measures the health of a Azure Application Gateway.
  codeBundle:
    {% if repo_url %}
    repoUrl: {{repo_url}}
    {% else %}
    repoUrl: https://github.com/runwhen-contrib/rw-cli-codecollection.git
    {% endif %}
    {% if ref %}
    ref: {{ref}}
    {% else %}
    ref: main
    {% endif %}
    pathToRobot: codebundles/azure-appgateway-health/sli.robot
  intervalStrategy: intermezzo
  intervalSeconds: 300
  configProvided:
    - name: AZ_RESOURCE_GROUP
      value: {{custom.az_resource_group}}
    - name: APPGATEWAY
      value: {{match_resource.resource.name}}
  secretsProvided:
    - name: AZ_USERNAME
      workspaceKey: {{custom.clientId}}
    - name: AZ_SECRET_VALUE
      workspaceKey: {{custom.clientSecret}}
    - name: AZ_TENANT
      workspaceKey: {{custom.tenantId}}
    - name: AZ_SUBSCRIPTION
      workspaceKey: {{custom.subscriptionId}}