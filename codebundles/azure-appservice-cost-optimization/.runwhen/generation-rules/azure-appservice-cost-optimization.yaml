apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  platform: azure
  generationRules:
    - resourceTypes:
        - azure_subscription_subscriptions
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: azure-appservice-cost-optimization
          qualifiers: ["subscription_id"]
          baseTemplateName: azure-appservice-cost-optimization
          levelOfDetail: basic
          outputItems:
            - type: slx
            - type: runbook
              templateName: azure-appservice-cost-optimization-taskset.yaml
            - type: sli
