# Basic DNS Health Test Configuration
# This configuration tests basic DNS resolution for private endpoints

variables:
  RESOURCE_GROUPS: "production-rg,network-rg"
  TEST_FQDNS: "myapp.privatelink.database.windows.net,myapi.privatelink.azurewebsites.net"

# Expected metrics:
# - DNS Resolution Success Rate: 100% (if all FQDNs resolve)
# - DNS Query Latency: < 100ms (typical for Azure DNS)
# - Private DNS Zone Health: 1 (healthy)
# - External DNS Resolver Availability: 100% (if external resolvers work)
