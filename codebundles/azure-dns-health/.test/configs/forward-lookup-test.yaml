# Forward Lookup Zone Test Configuration
# This configuration tests forward lookup zones and conditional forwarders

variables:
  RESOURCE_GROUPS: "production-rg,network-rg"
  TEST_FQDNS: "myapp.privatelink.database.windows.net"
  FORWARD_LOOKUP_ZONES: "internal.company.com,corp.local"
  FORWARD_ZONE_TEST_SUBDOMAINS: "dc01,mail,web"
  DNS_RESOLVERS: "8.8.8.8,1.1.1.1,10.0.0.4"

# Expected behavior:
# - Tests forward lookup zones: internal.company.com, corp.local
# - Tests subdomains: dc01.internal.company.com, mail.internal.company.com, web.internal.company.com
# - Tests specific DNS resolvers: 8.8.8.8, 1.1.1.1, 10.0.0.4
# - Should detect if forward lookup zones are properly configured
