apiVersion: runwhen.com/v1
kind: ServiceLevelX
metadata:
  name: {{slx_name}}
  labels:
    {% include "common-labels.yaml" %}
  annotations:
    {% include "common-annotations.yaml" %}
spec:
  imageURL: https://storage.googleapis.com/runwhen-nonprod-shared-images/icons/gcp/apigee_api_platform/apigee_api_platform.svg
  alias: GCP Apigee Environment and Edge Health for Project {{project.name}}
  asMeasuredBy: Findings raised by the runbook across org/environment state, instance attachment coverage, environment group attachments and hostname routing, keystore certificate expiry, target server reachability, southbound VPC peering / Private Service Connect, and instance capacity for the Apigee org in project {{project.name}}
  configProvided:
    - name: GCP_PROJECT_ID
      value: "{{project.name}}"
    # The matched resource IS the Apigee organization, so its name is known at
    # render time. This is populated before anything runs and stays correct even
    # when a live discovery call fails -- unlike resolving it at run time.
    # custom.apigee_org remains an override for the rare case where the indexed
    # name is not what the checks should target.
    - name: APIGEE_ORG
      value: '{{custom.apigee_org | default(match_resource.resource.name)}}'
  owners:
    - {{workspace.owner_email}}
  statement: The Apigee organization's environments should be ACTIVE and attached to runtime instances, environment groups should be attached with routed hostnames, keystore certificates should be far from expiry, and target servers should be enabled and reachable.
  additionalContext:
    {% include "gcp-hierarchy.yaml" ignore missing %}
    qualified_name: "{{ match_resource.qualified_name }}"
  tags:
    {% include "gcp-tags.yaml" ignore missing %}
    - name: service
      value: apigee
    - name: scope
      value: Project
    - name: access
      value: read-only
