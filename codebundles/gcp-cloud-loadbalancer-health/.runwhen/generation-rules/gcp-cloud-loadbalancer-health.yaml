apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  platform: gcp
  generationRules:
    - resourceTypes:
        - gcp_compute_forwarding_rules
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: gcp-cloud-loadbalancer-health
          qualifiers: ["project"]
          baseTemplateName: gcp-cloud-loadbalancer-health
          levelOfDetail: basic
          outputItems:
            - type: slx
            - type: sli
            - type: slo
            - type: runbook
              templateName: gcp-cloud-loadbalancer-health-taskset.yaml
