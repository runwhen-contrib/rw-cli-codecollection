apiVersion: runwhen.com/v1
kind: Runbook
metadata:
  name: {{slx_name}}
  labels:
    {% include "common-labels.yaml" %}
  annotations:
    {% include "common-annotations.yaml" %}
spec:
  location: {{default_location}}
  description: Runs tasks to validate GCP Cloud Spanner backup and data-protection governance, including backup recency, backup expiration, PITR configuration, deletion protection, IAM access, and encryption.
  codeBundle:
    {% if repo_url %}
    repoUrl: {{repo_url}}
    {% else %}
    repoUrl: https://github.com/runwhen-contrib/rw-cli-codecollection.git
    {% endif %}
    {% if ref %}
    ref: {{ref}}
    {% else %}
    ref: main
    {% endif %}
    pathToRobot: codebundles/gcp-cloudspanner-backup-governance/runbook.robot
  configProvided:
    - name: GCP_PROJECT_ID
      value: "{{project.name}}"
    - name: BACKUP_RECENCY_THRESHOLD_HOURS
      value: "24"
    - name: BACKUP_EXPIRY_WARNING_DAYS
      value: "3"
    - name: PITR_MINIMUM_DAYS
      value: "1"
    - name: REQUIRE_CMEK
      value: "{{custom.require_cmek | default('false')}}"
  secretsProvided:
  {% if wb_version %}
    {% include "gcp-auth.yaml" ignore missing %}
  {% else %}
    - name: gcp_credentials
      workspaceKey: AUTH DETAILS NOT FOUND
  {% endif %}
