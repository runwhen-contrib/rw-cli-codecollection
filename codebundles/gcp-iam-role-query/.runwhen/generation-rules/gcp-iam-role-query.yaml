apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  platform: gcp
  generationRules:
    - resourceTypes:
        - project
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: gcp-iam-role-query
          qualifiers: ["project"]
          baseTemplateName: gcp-iam-role-query
          levelOfDetail: basic
          outputItems:
            - type: slx
            - type: runbook
              templateName: gcp-iam-role-query-taskset.yaml
