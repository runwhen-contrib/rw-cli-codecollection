apiVersion: runwhen.com/v1
kind: Runbook
metadata:
  name: {{slx_name}}
  labels:
    {% include "common-labels.yaml" %}
  annotations:
    {% include "common-annotations.yaml" %}
spec:
  location: {{default_location}}
  description: Runs tasks to audit a GCP project's configuration health including PERMISSION_DENIED activity, IAM policy changes, org policy constraint violations, and audit log configuration.
  codeBundle:
    {% if repo_url %}
    repoUrl: {{repo_url}}
    {% else %}
    repoUrl: https://github.com/runwhen-contrib/rw-cli-codecollection.git
    {% endif %}
    {% if ref %}
    ref: {{ref}}
    {% else %}
    ref: main
    {% endif %}
    pathToRobot: codebundles/gcp-project-configuration-audit/runbook.robot
  configProvided:
    - name: GCP_PROJECT_ID
      value: "{{project.name}}"
    - name: LOOKBACK_WINDOW
      value: "{{custom.lookback_window | default('P7D')}}"
    - name: PERMISSION_DENIED_THRESHOLD
      value: "{{custom.permission_denied_threshold | default('10')}}"
    - name: ORG_ID
      value: "{{custom.org_id | default('')}}"
  secretsProvided:
  {% if wb_version %}
    {% include "gcp-auth.yaml" ignore missing %}
  {% else %}
    - name: gcp_credentials
      workspaceKey: {{custom.gcp_credentials_secret | default("gcp_credentials")}}
  {% endif %}
