---
# KWOK-managed fake Node, cordoned at creation time. check-karpenter-
# nodepool-nodeclaim-status.sh flags cordoned nodes via its
# spec.unschedulable branch.
apiVersion: v1
kind: Node
metadata:
  name: kwok-cordoned-0
  annotations:
    kwok.x-k8s.io/node: fake
  labels:
    kubernetes.io/role: agent
    type: kwok
    test.runwhen.com/fixture: node-cordoned
spec:
  unschedulable: true
  taints:
    - key: kwok.x-k8s.io/node
      value: fake
      effect: NoSchedule
