apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  generationRules:
    - resourceTypes:
        - service
      matchRules:
        - type: pattern
          pattern: "pgbouncer"
          properties: ["name"]
          mode: substring
      slxs:
        - baseName: k8s-pgbouncer-prom
          shortenedBaseName: k8s-pgb-prom
          qualifiers: ["resource", "namespace", "cluster"]
          baseTemplateName: k8s-pgbouncer-prometheus-health
          levelOfDetail: basic
          outputItems:
            - type: slx
            - type: runbook
              templateName: k8s-pgbouncer-prometheus-health-taskset.yaml
