apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  generationRules:
  - resourceTypes:
      - postgresclusters.postgres-operator.crunchydata.com
    matchRules:
    - type: pattern
      pattern: ".+"
      properties: [name]
      mode: substring
    slxs:
      - baseName: postgres-operations
        qualifiers: ["resource", "namespace", "cluster"]
        baseTemplateName: k8s-postgres-operations-crunchy
        levelOfDetail: detailed
        outputItems:
          - type: slx
          - type: runbook
            templateName: k8s-postgres-operations-crunchy-taskset.yaml
